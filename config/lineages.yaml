# Engine families. Independence is the whole basis of Tier 2 and Tier 4 evidence,
# so a lineage claim here is a hypothesis, not a fact.
#
# CLAUDE.md section 4: "We maintain config/lineages.yaml and *estimate*
# independence empirically from gold errors." The declared grouping below only
# sets the prior; the pairwise error-correlation matrix (phi per pair, T10)
# measured on gold overrides it. If the best pair exceeds phi 0.5, independent
# consensus is fiction and the evidence weights must be cut accordingly.

lineages: []

# Example shape:
#
# lineages:
#   - id: aws-textract
#     backbone: "AWS Textract OCR stack"
#     notes: "Assumed independent of LLM-native extractors; verify on gold."
#     known_shared_components: []
